# Nexus — local Cursor HTTP bridge (optional). See prompt-os/docs/cursor-bridge.md
# After `npm run bridge:mock`, point the side panel bundle at the mock server:
# VITE_PROMPT_OS_CURSOR_BRIDGE_URL=http://127.0.0.1:17373
