import { PromptOSPanel } from '@/components/promptOs/PromptOSPanel'

/** Side panel tab: full instructional contract builder (see {@link PromptOSPanel}). */
export function InstructionalContractTab() {
  return <PromptOSPanel />
}
